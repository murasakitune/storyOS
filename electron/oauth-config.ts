// This placeholder is replaced during electron:compile with values read from
// .env.local. It keeps OAuth client credentials in the Electron main bundle
// and out of the Vite renderer bundle and IPC surface.
export const GOOGLE_DESKTOP_CLIENT_SECRET = "";
